version: "3"

services:
  web:
    image: ghcr.io/gcnyin/crypto-coins-market:0.1.0
    restart: always
    ports:
      - 9001:9000
    networks:
      - database
    depends_on:
      - postgres
    environment:
      - DB_JDBC_URL=jdbc:postgresql://postgres:5432/test_db
      - DB_USERNAME=postgres
      - DB_PASSWORD=12345
      - JAVA_OPTS=${JAVA_OPTS}
  postgres:
    image: postgres:13.3-alpine
    restart: always
    networks:
      - database
    environment:
      - POSTGRES_DB=test_db
      - POSTGRES_PASSWORD=12345
networks:
  database:
